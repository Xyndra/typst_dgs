[package]
name = "dgs-wasm"
version = "0.1.0"
edition = "2021"
description = "WASM plugin entry point for typst_dgs"

[lib]
crate-type = ["cdylib"]

[dependencies]
dgs = { workspace = true }
serde = { workspace = true }
ciborium = { workspace = true }
wasm-minimal-protocol = "0.2.0"
