[package]
name = "dgs"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["typst_dgs contributors"]
license = "MIT"
description = "Dynamic Geometry Software for Typst — plot shapes, equations, and geometric constructions with a GeoGebra-like coordinate system."

[template]
path = "."
entrypoint = "examples/demo.typ"
